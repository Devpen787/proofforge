# M00 Preflight Evidence — 2026-09-17

Mission: **M00 — Dogfood current Logos Basecamp module creation/loading path**

Evidence status: `toolchain-verified-tutorial-pending`

## Test machine

- Host: `Devinsons-MacBook-Pro.local`
- OS: macOS 26.6.2
- Build: `25G83`
- Darwin: `25.6.0`
- Architecture: `arm64`
- Kernel release target: `RELEASE_ARM64_T6030`
- Git: `2.50.1 (Apple Git-155)`
- Initial Nix state: not installed (`zsh: command not found: nix`)
- Installed Nix version: `2.35.2` via official multi-user installer

## Initial preflight commands

```bash
uname -a
uname -m
sw_vers
nix --version
git --version
nix show-config 2>/dev/null | grep -E 'experimental-features|system' || true
```

## Initial observed output

```text
Darwin Devinsons-MacBook-Pro.local 25.6.0 Darwin Kernel Version 25.6.0: Fri Jul 31 19:16:36 PDT 2026; root:xnu-12377.161.14~5/RELEASE_ARM64_T6030 arm64

arm64

ProductName:        macOS
ProductVersion:     26.6.2
BuildVersion:       25G83

zsh: command not found: nix

git version 2.50.1 (Apple Git-155)
```

## Nix installation evidence

Command used:

```bash
curl -L https://nixos.org/nix/install | sh -s -- --daemon
```

Observed installer facts:

- downloaded `nix-2.35.2-aarch64-darwin.tar.xz` from `releases.nixos.org`;
- switched to the multi-user installer;
- configured `/nix` using a dedicated APFS Nix Store volume on macOS;
- encrypted the Nix Store volume;
- created the `nixbld` build group and 32 hidden, login-disabled build users;
- installed Nix at `/nix/store/xbb13wrh7lv306phbapc006hfsscjd97-nix-2.35.2`;
- added Nix shell profile hooks to the system shell configuration;
- installed the default Nix profile, manual, and CA certificates;
- configured and started the `nix-daemon` LaunchDaemon;
- completed with `Alright! We're done!`.

The installer emitted `chown: /nix/.Trashes: Operation not permitted` while preparing the volume, but continued through installation and completed successfully. Treat this as a recorded installation warning unless later verification shows an actual functional problem.

The installer explicitly states that Nix will not work in already-active shell sessions until they are restarted.

## Fresh-shell verification

Observed in a new Terminal session:

```text
$ nix --version
nix (Nix) 2.35.2
```

The first `nix config show` and `nix flake metadata` attempt stopped at the expected client feature gate because `nix-command` and `flakes` were not enabled yet.

## Feature configuration

Configured in the user's Nix config:

```text
experimental-features = nix-command flakes
```

## Logos module-builder resolution

After enabling `nix-command` and `flakes`, this command succeeded:

```bash
nix flake metadata github:logos-co/logos-module-builder --no-write-lock-file
```

Observed source resolution:

- Resolved URL: `github:logos-co/logos-module-builder`
- Locked revision: `9bbe0aa4c40d93669ecd9847bf171c77ce31f651`
- Description: `Logos Module Builder - Shared library for building Logos modules with minimal boilerplate`
- Nix store path: `/nix/store/al65pvxcynrs39d2f820xmnhjirpr0r9-source`
- Last modified: `2026-09-17 09:47:44`
- Fingerprint: `3754b64e9ebdd682f933aae161901dbe04af41959cb427660438bb96790af501`

The flake also resolved its current Logos dependency graph, including the Logos C++ SDK, design system, module library, Logos protocol, Qt SDK, Rust SDK, standalone app, test framework, LGX bundler, and package tooling.

## Interpretation

The local prerequisite gate is now passed:

- macOS Apple Silicon lane identified;
- Git available;
- Nix 2.35.2 installed and available in a fresh shell;
- `nix-command` enabled;
- `flakes` enabled;
- the official Logos `logos-module-builder` flake resolves successfully from GitHub and is content-addressed into the Nix store.

This is a toolchain-resolution proof only. It does **not** yet prove that a Logos module builds, packages, loads in Basecamp, or survives restart.

## Next proof target

Proceed with the official executable Logos tutorial path:

1. create a clean `~/Developer/logos-m00` workspace;
2. clone `logos-co/logos-tutorial`;
3. record the exact tutorial HEAD SHA;
4. run the official `tutorial-qml-ui-app.test.yaml` via `logos-doctest` with preserved output and HTML report;
5. preserve the first-pass results without silently fixing failures;
6. continue to LGX packaging and Basecamp loading if the tutorial chain passes far enough.

No Logos build claim is made from setup evidence alone.
